# 历史验证记录

最后记录：2026-09-24。当次环境为 Windows、Python 3.12.14 和 uv 锁定依赖；各次运行使用的模型、配置与数据版本以对应记录为准。

本文只保存已执行的测试、真实运行和内容抽查结果。现行功能与数据版本以[技术设计](technical-design.md)为准；旧数据版本的真实产物不作为现行修订基线。尚无用户人工修订耗时对照，不能据此声称整体质量验收通过。

## 首次素材探测

首次验证的 Bilibili 单课时缓存为《生成式软件工程》“欢迎来到未来”（BV1pb8o6yE8f，第 1 P），约 5,999.3 秒。视频为 H.264、1280 × 410、30 FPS，音频为 AAC、48 kHz 双声道；两个独立 `.m4s` 媒体文件均有 9 字节 ASCII `0` 前缀，校验后可解码。`inspect --decode` 的前中后抽查通过，检查前后媒体 SHA-256 不变；当次未据此推断全片解码通过。

另一个早期数学缓存（BV1sH4y1S7gm）约 2,697.566 秒，视频为 H.264 1920 × 1080，音频为 AAC 48 kHz 双声道。10–20 分钟片段用于线性方程组、二次曲线参数和矩阵板书评估。私人素材、转写与图片保存在忽略目录。

## 默认检查

- 2026-09-24 测试精简后执行 `uv run --no-sync pytest -q`：99 项通过，13.75 秒；Ruff 规则和格式检查通过。测试函数由 95 个减至 87 个，参数展开用例由 117 项减至 99 项，测试代码净减少 126 行。删除重复帮助/参数组合、固定文案和辅助函数自证式检查；真实换图流程继续核对图注手改、CRLF、图片行冲突与版本携带，配置修订流程同时核对供应商覆盖和合法空值。路径保护、文件锁、取消、证据与时间映射回归保留。本轮只调整测试与记录，没有真实模型、GPU 或整课运行。
- 2026-09-24 项目审查后执行 `uv run --no-sync pytest -q`：117 项通过，18.45 秒；Ruff 规则和格式检查通过。新增检查覆盖修订基线合法空值的保留、本地 ASR 迭代期间取消的错误语义；性能报告覆盖并发请求累计耗时与空日志，终端检查改为验证实际显示内容。清理前后图文请求 JSON schema 完全一致。文档本地链接、章节锚点、CLI 参数示例与配置示例通过检查。本轮未调用真实模型、GPU 或重跑私人课程，未重验整课质量和实际加速幅度。
- 2026-09-24 执行 `uv run --no-sync pytest -q`：115 项通过；`uv run --no-sync ruff check .` 和 `uv run --no-sync ruff format --check .` 通过。新增离线重建用例从冻结证据和草稿生成 r001，分别检测证据输入及草稿改动；现有文字修订用例也核对手改基线、图注和整章修订的离线重建。测试没有模型请求或整课重跑。
- 既有环境与构建检查：`uv sync --locked`、`uv build` 与 2026-09-09 的 `uv build --offline` 通过，源码包和 wheel 无输出、私人素材或凭据。2026-09-14 增加可选本地依赖时，`uv build` 及 wheel 内容检查通过，包含 local_asr 和价格文件。
- 2026-09-14 执行 `uv run --no-sync pytest -q`：112 项通过，13.96 秒。使用自造音视频和确定性模型替身，不访问网络、私人素材、GPU 或模型权重。
- `uv run ruff check .` 和 `uv run ruff format --check .`：通过。
- Windows 覆盖 OS 文件锁、目录 junction 逃逸、媒体前缀、PTS/音轨偏移、静音与原课时间映射、周期板书候选、错误引用、取消、失败发布、并发编辑、锚点冲突和修订字节保护。
- 尾部采样覆盖小数起止时间、实际帧 PTS、无范围内帧时拒绝，以及精确换图的原有边界。列表和引用内代码块通过完整转换与 CRLF 修订；未闭合围栏在正文校验阶段拒绝。
- 连续换图、单块和整章修订验证疑点更新、范围外及全局提示保留、`uncertain` 正文进入核对清单；导出校验拒绝不存在的关联目标。

测试替身只证明输入输出约束及失败行为。没有测量真实 ASR 的字错率，也没有对生成代码执行或对公式做自动证明。

转换和文字修订的新产物保存模型前后的业务数据边界；开发侧只读回放已由自造素材验证。四个历史整课基线缺少调用前证据包和采用草稿的成套冻结记录，本轮未据此声称它们已通过新回放，也未复验真实模型输出的内容质量。

2026-09-09 的离线验证覆盖部分媒体探测失败、元数据错误不影响媒体打开状态、JSON 非有限数值拒绝且保留原文件，以及 DeepSeek/Qwen 图文配置下 ASR 用量始终归属 Qwen。这些离线检查不调用模型；真实整课运行和审阅单独记录在本文末尾。

终端进度验证使用自造媒体和事件替身：确认音频完成区间、截图扫描/保存与章节总数；100 次请求重定向后仅保留阶段摘要和请求汇总，完整日志和用量不丢失。覆盖 80 列交互布局、请求总量估算、去重计时、累计重试和思考活动、失败章节计数、重试、详细模式和中断清理；CLI 帮助已确认包含 `--verbose`。四课整段运行已验证重定向阶段摘要、失败章节和用量汇总；交互终端覆盖仍以离线测试为主。

强制覆盖验证使用自造转换目录：CLI `-f` 删除旧文件并生成新结果；默认拒绝覆盖；预检错误、目录锁冲突和受保护路径被拒绝。未删除真实课程输出。

## Token meter 验证

2026-09-09，真实转换编程课 00:59:40–00:59:50，输出在 `output/meter-smoke-20260909/`。Qwen ASR 返回 10 秒音频、253 输入 / 43 输出 token；图文返回 3173 输入 / 837 输出 token、缓存命中 0。终端与 `usage.json` 记录总计 4306 token，Estimated 费用 0.006998 元（ASR 0.0022、图文 0.004798）。金额采用[价格快照](../src/video_learner/common/prices.toml)，没有读取供应商账单。

用同一快照复算已有完整转换日志，包含结构修复重试、排除后续修订：编程课 566703 token，估算 1.773188 元；数学课 391520 token，估算 0.899759 元。旧日志缺少缓存细分，图文输入全部按普通输入价估算。报告在 `artifacts/evaluations/token-cost-20260909/`，原转换目录未改写。

离线测试覆盖重试计费、未知请求不按免费处理、缓存输入不重复计算、显式零价、ASR 按秒、DeepSeek 北京时间高峰/午休/周末、CLI 汇总和失败目录保存。构建后的 wheel 已检查包含计价模块及价格 TOML。DeepSeek 时段计价仅离线验证，本轮真实请求使用 Qwen。

## 阅读与分段策略验证

2026-09-08，新策略完成数学 00:35:30–00:39:30、编程 00:58:00–01:02:00 两次真实转换，并各执行两次章节精简修订。阅读入口分别是 `output/math-readable-sample/revisions/r003/notes.md` 和 `output/programming-readable-sample/revisions/r003/notes.md`；原整课结果保持不变。

| 检查 | 数学 | 编程 |
| --- | --- | --- |
| 音频切片 / 停顿切点 | 9 片 / 8 处 | 9 片 / 7 处 |
| 章边界 | 00:38:18 | 01:01:10 |
| 同范围旧候选 / 新候选 | 9 / 10 | 13 / 16 |
| 最终图注字符数 | 0、0 | 43、25、0 |
| 音频连续、PTS、引用、锚点、版本哈希 | 通过 | 通过 |
| 两次修订非目标字节及独立复制图片 | 通过 | 通过 |

以切点前后各 100 毫秒的波形计算 RMS，中位数由固定切分的数学 0.156、编程 0.221 降为新策略的 0.00023、0.00229。说明本次切点更安静，不代表已经证明识别准确率或语义分段更好。两段各比固定 30 秒切分增加一次识别调用。

自造逐行板书在第 14 秒翻页，新采样保留第 12 秒的完整板书和第 18 秒的末页。真实编程样本选中 00:59:48 稳定幻灯片；数学样本仍保留部分上一话题板书，跨章比喻也有重复。当前接受这些冗余，不做复杂场景分类或语义边界推断。正文来源杂项已移入独立文档，图注在真实复验中明显缩短；这轮没有重跑整课，也没有完成内容正确性人工验收。

详细数据在 `artifacts/evaluations/readability-segmentation-20260908.json`，独立复制在 `artifacts/evaluations/readable-portable-20260908/`。运行过程与提示调整记录在实现笔记中。

## 真实片段与版本

下表使用各样本最新已验证版本；全部位于本机忽略目录。PPT 和编程片段来自同一约 100 分钟课程，数学片段来自另一个约 45 分钟课程。

| 场景 | 原课范围 | 最新版本 | 章节 / 内容块 / 插图 | 真实修订 |
| --- | --- | --- | --- | --- |
| 编程操作 | 00:45:00–00:55:00 | `output/programming-sample/revisions/r003/` | 4 / 42 / 18 | 章节整理为 USB 故障排查，再指定 00:49:50 换图 |
| PPT | 00:20:00–00:25:00 | `output/ppt-sample/revisions/r002/` | 2 / 13 / 5 | 压缩课程说明，保留模型概念与案例 |
| 数学板书 | 00:10:00–00:20:00 | `output/math-final/revisions/r003/` | 4 / 24 / 7 | 明确系数归一化条件和五点问题，再调整 00:15:30 板书截图 |

逐个验证了以上样本的 r001 和全部修订版本：章节覆盖连续、证据 ID 存在、时间合法、帧时间与 PTS 一致、图片可解码。各版本仅复制 `notes.md` 和 `assets/` 后引用仍完整。所有已发布 Markdown 哈希不变，每次修订的目标范围外字节一致，源文件哈希与转换前相同。

本机机械检查数据：`artifacts/evaluations/final-mechanical-checks.json`。原始模型响应、各阶段耗时和 token 用量位于各工作目录 `.work/`；凭据未写入这些记录。

首次转换的已记录阶段耗时和服务返回用量如下，不含后续修订、人工阅读及首次安装。输入 token 包含图像等服务计量项，不等于纯文字长度。

| 样本 | 阶段耗时合计 | API 请求 | 输入 / 输出 token |
| --- | --- | --- | --- |
| 编程 10 分钟 | 261.8 秒 | 4 | 34,832 / 5,381 |
| PPT 5 分钟 | 121.2 秒 | 2 | 14,150 / 2,877 |
| 数学 10 分钟 | 265.1 秒 | 4 | 27,736 / 4,049 |

不同片段、系统负载和服务响应会改变耗时；这批早期运行仅记录用量，未生成费用汇总。详细记录在 `artifacts/evaluations/final-metrics.json`。

## 内容与时间抽查

以下为代理对原图、转写与文稿的抽查，不是用户人工验收，也不是全片逐帧标注。抽帧的实际时间已通过 PTS 机械核对；此处早期本地识别产物的句段边界为模型估计；当前 ASR 按真实音频窗口引用，见[数据契约](technical-design.md#3-固定证据提取)。

| 抽查点 | 观察 | 限制与处理 |
| --- | --- | --- |
| PPT 00:22:00 | 当时输出的截图完整显示 SEM 案例，图注中的电子束扫描比喻及因果链可在图中找到 | 文稿仍较长，个别口语转写术语需核对 |
| 编程 00:49:50 | 原画面含设备检查命令、USB 设备列表和恢复结果；修订图保留这些信息 | 截图中的命令是课程数据，未在本机执行；个别专名仍有 ASR 错字 |
| 数学 00:15:30 | 当时输出的截图中可见二次曲线六项公式，与图注对应 | r002 已明确除以某系数需要非零；其他章节仍可能省略适用条件 |
| 数学中间板书 | 10 分钟保留 20 张候选，文稿包含方程、行列式、二次曲线及矩阵阶段 | 仍有近似重复图；短于采样间隔的临时笔迹可能遗漏 |
| 数学文本 | 原课叙事与一般化解释可形成连续讲义 | 人名音译不统一、历史叙述与“可能是”等推测仍需回看；不能将叙事整理当作数学证明 |

已知内容问题包括：ASR 专名误识别、公式条件省略、图注过长、近似重复插图，以及模型未主动列出的疑点。`review.md` 提供回看入口但不保证发现所有错误。LaTeX 渲染依赖阅读器；较早样本有 Unicode/行内公式写法。局部修订可纠正已发现的问题，不能自动证明其余段落正确。

## 整课与验收边界

约 100 分钟整课 `output/full-course-final/` 已完成：34 章、294 个块、133 张插图，2,381 条转写和 265 张候选图。范围覆盖 0–5,999.300125 秒，来源、PTS、图片、复制和源文件哈希检查通过。95 项疑点待核对。

阶段耗时合计 3,405.6 秒（56 分 46 秒），其中本地 ASR 2,782.6 秒、采样 107.5 秒；DeepSeek 共 40 次请求（含修复），输入 396,671、输出 56,199 token。最大单进程峰值工作集 762,339,328 字节（约 727 MiB）；5 秒采样的合计工作集峰值约 641 MiB。资源记录不包含系统文件缓存和其他应用。

| 用户故事 | 当前证据 | 验收边界 |
| --- | --- | --- |
| US-01 | 两个真实缓存只读检查与自造普通媒体验证 | 不承诺所有客户端缓存格式 |
| US-02 | 三类真实片段和 34 章整课成功 | 不提供 P1 断点恢复 |
| US-03 | Markdown、证据、图片及全部版本可携带性验证 | 排版由 Markdown 阅读器决定 |
| US-04 | 三类内容抽查、真实文字与截图修订 | 条件、代码和步骤仍需人工验收 |
| US-05 | 证据与时间约束、来源类别、疑点输出 | 证据存在不能证明语义蕴含 |
| US-08 | 真实章节修订及离线手改/并发/冲突检查 | 被用户删除或破坏的锚点需人工处理 |
| US-10 | 历史真实换图、版本保护及复制；当前全帧换图离线验证 | 自然语言重新选图属于 P1 |

尚未完成用户人工学习/修订耗时与纯手工整理的对照，也未取得三类课程的完整人工正确性标注。运行时间与资源测量仅描述本机本次实验。


## 云端 ASR 验证

Qwen 在两段各 60 秒音频的实测耗时为 3.135 秒和 1.578 秒。本地识别及参数比较记录保留在实现笔记中。当次整课产物属于旧数据版本的历史运行结果；阅读与现行修订能力分别按对应产物和[技术设计](technical-design.md#5-数据与独立版本)判断。

Qwen 数学结果中的莱布尼茨、克莱姆法则、未知数和线性方程组比本地对应文本更准确；编程专名仍有误识别。这是代理对比抽查，没有完整人工真值，不报告 WER。各次系统负载及云端延迟有波动，不能将单段倍数当成整课保证。

Qwen 另完成 10–20 分钟数学片段的完整 CLI 转换，产物 `output/qwen-math-sample/`：20 个真实音频窗口、4 章。转写阶段 31.8 秒，之后采样约 48.8 秒、组织约 34.5 秒。切片来源显式标为非句级对齐。数据在 `artifacts/evaluations/qwen-asr-comparison.json` 和 `asr-speed-comparison.json`，原始转写保留在忽略目录。

当时的 Qwen ASR 路径验证了缺少凭据的预检，以及图文供应商选择不改变原始音频切片时间。现行修订明确拒绝旧数据版本，见[当前数据契约验证](#当前全帧数据契约验证)。


## Qwen 3.8 Flash 图文验证

2026-09-07，阿里云 DashScope 的 `qwen3.8-flash`，思考开启、流式响应、思考预算 1024。完成数学视频 14:30–15:30 的实际 CLI 转换：1 章、3 个文字块、1 张图；Qwen ASR 约 7.2 秒，采样约 5.3 秒，图文组织单次请求 18.0 秒（输入 6049、输出 1415 token）。产物在 `output/qwen38-vision-final/`。

对已有 DeepSeek 数学产物的 `fig-001-002` 切换 Qwen 修订图注，单次模型请求 10.4 秒（输入 4210、输出 718 token），生成 `output/qwen-math-sample/revisions/r002/`。原始 r001 哈希不变，目标范围外字节一致，图片仍使用原选中帧；没有重新转写。

上述产物通过原素材哈希、证据时间、图片校验、相对资源依赖及配置指纹检查，凭据路径未持久化到配置。代理抽查 15:00 板书与生成图注，图中的行列式阶段、未知数记号及人物与图注基本对应；没有完整人工正确性标注，不由一分钟样本推断整课质量。机械记录在 `artifacts/evaluations/qwen38-vision-checks.json`。

新增离线检查覆盖图片编码、仅收集最终正文、usage 尾块、未完成输出拒绝、流中断重试丢弃残片、结构错误修复、固定服务地址、供应商模型/凭据切换，以及跨供应商修订的非目标字节保护。当次全套 42 项通过，Ruff 和构建检查通过。


## 2026-09-08：两个 case 的完整 Qwen 转换

2026-09-08 当次产物为 `output/programming-full/` 和 `output/math-full/`；这些目录及当时的 `output/README.md` 已不在当前工作目录。现存四门完整课时的阅读入口见[内容质量记录](content-quality.md#审阅范围与交付)。

| Case | 原视频范围 | 章节 / 图 | 阶段耗时合计 | ASR 窗口 / 图文请求 |
| --- | --- | --- | --- | --- |
| 编程课 | 00:00:00–01:39:59 | 34 / 92 | 1920.2 秒 | 200 / 44 |
| 数学课 | 00:00:00–00:44:57 | 15 / 28 | 894.6 秒 | 90 / 18 |

图文请求数包含格式修复；两份任务独立运行，阶段耗时不能相加当成总等待时间。原视频范围在索引中保留整数微秒，表格展示到秒。所有章节完成，来源指纹未变，证据时间、图片缓存、文稿锚点、相对资源依赖和 r001 哈希检查通过。评审内容保持为本次模型生成的初稿，等待用户意见，未标记人工正确性验收通过。

本次配置：Qwen 3.8 Flash 思考开启、思考预算 1024、输出上限 12000、超时 180 秒、图文调用上限 100；ASR 使用默认 30 秒窗口，保留完整画面。配置和机械检查数据在 `artifacts/evaluations/full-cases-20260908.toml`、`full-cases-20260908.json`。本次未更改业务代码。


## 性能分析与计时验证

两个完整 case 的阶段归因和本地 cProfile 结果见[整课性能分析归档](implementation-notes.md#2026-09-08整课性能分析归档)。新增离线检查覆盖嵌套事件不重复计时、转换与修订分离、流式首块/思考/正文计时、运行 ID、响应关联及流内服务错误不泄漏原始消息。当次全套 45 项通过。原有两份讲义保持不变，未为 profiling 重跑整课。一次独立云端探测被供应商内容检测中止，仅记录部分延迟，不作为成功响应基准。


## 全帧缓存与阶段摘要验证

自造媒体验证每个截图证据仅保存一张原分辨率完整帧，逐字节匹配直接 PNG 编码结果，实际帧时间、PTS 和哈希一致。阶段摘要覆盖音频片数与重试请求分别计数、扫描/候选数量、图文部分失败显示，以及离线转换日志中的章节和插图数量。Ruff 检查通过；未调用真实模型、重跑整课或测量真实加速幅度。


## ASR 并发验证

ASR 仅使用离线替身，覆盖有界预取和请求重叠、乱序返回与空文本保存、停顿切片连续性、全局预算竞争、重试编号及响应关联、取消唤醒退避、调用方异常后工作线程收尾，以及日志/终端回调串行化。图文整理流程和提示词保持不变。未验证真实供应商并发限流或 ASR 加速幅度；本次全部操作 API 调用数为 0。

当前单文件截图缓存只记录实际运行与一致性验证；此前双文件缓存的 A/B 测量已归档到[实现笔记](implementation-notes.md#2026-09-09双文件缓存历史测量)，不代表当前实现的性能。


## 当前全帧数据契约验证

工作清单与讲义索引结构版本为 2，提取版本为 4。离线验证旧版本被明确拒绝且不写入新修订，新版本的精确换图、手改图注与非目标字节、独立图片复制、缓存篡改诊断继续通过。每张证据只生成一个 PNG，直接编码字节、实际时间、PTS 与 SHA-256 一致。

另使用同一真实编程课的 00:49:00–00:51:00 进行本地回放：扫描 60 帧，保存 12 个 PNG；与此前完整画面输出相比，全部扫描记录、选帧、时间证据和图片哈希相同，源视频哈希不变。单次运行 12.91 秒仅作本次功能复核，未控制缓存与系统负载，不用于比较加速幅度。明细位于 artifacts/evaluations/full-frame-only-20260909/verification.json。本次没有模型请求或整课重跑。

已移除的接口及数据字段不再出现在运行代码、测试、配置示例和当前技术/用户文档中；删除历史功能的决定保留在实现笔记。Pillow 和 NumPy 分别继续用于 PNG 编码与图片校验、缩略图比较与音频处理，没有仅服务于已删除功能的第三方依赖；依赖声明和锁文件保持原样。

CLI 帮助及参数拒绝检查通过：旧选项和 TOML 字段均报错，不会被静默接受。运行代码、测试、工具、配置和当前文档完成残留扫描；本地 Markdown 链接与 `git diff --check` 通过。

## 可选本地 ASR 与指定四课样本

本轮仅选择以下四个视频，每个先识别 60 秒。输入缓存只读，原音频与转写结果保存到忽略目录 `artifacts/asr/`。

| 课程 | BVID / 分 P | 缓存目录末级 | 片段 | 语言 |
| --- | --- | --- | --- | --- |
| 叔本华（一） | BV1Ra41147Rj / 1 | 550041191 | 05:00–06:00 | zh |
| Stanford CS336：Lecture 7 Parallelism | BV1fvhK6hExg / 7 | 41224700231 | 05:00–06:00 | en |
| 南京大学生成式软件工程：欢迎来到未来 | BV1pb8o6yE8f / 1 | 41301577497 | 45:00–46:00 | zh |
| 抽象代数：1-1-1 | BV1iQpEe8EoN / 1 | 25635329216 | 05:00–06:00 | zh |

CPU small/int8 的四段识别完成，非零起点和窗口映射保留。人工真值未标注，不能提供 WER：文本抽查中英文并行计算概念可辨认；中文存在繁简混用、Galois → Galaxy、线性空间等术语错误，短尾窗也可能截断词句。结构通过不等于内容正确；图像证据和后续核对仍有必要。CUDA 四段实机识别已完成，术语仍存在错识。

### 2026-09-14 短片段性能测量

CPU 测量环境为 Ryzen 7 7700（8 核 16 线程）、small/int8/beam=1，叔本华第一课 05:00–06:00：

| 算子线程 × worker | 首次推理秒 | 热运行秒 | 热运行实时率 | 峰值 RSS MiB |
| --- | ---: | ---: | ---: | ---: |
| 4 × 1 | 6.391 | 6.203 | 0.103 | 650 |
| 8 × 1 | 4.954 | 5.109 | 0.085 | 704 |
| 4 × 2 | 4.359 | 4.157 | 0.069 | 648 |

同样使用 4 × 2，抽象代数、CS336 的 05:00–06:00，以及软件工程的 45:00–46:00，首次转写分别为 5.907、3.891、4.063 秒。模型加载约 1.6–2.3 秒，推理耗时包含解码与落盘；只测试所列四课，不外推整课性能。

CUDA 测量使用 RTX 4070 系列 12GB、CUDA 12/cuDNN 9、large-v3-turbo/int8_float16、单 worker。同样四段音频的首次推理为 1.531、1.593、1.031、1.437 秒；叔本华热运行 1.375 秒，实时率 0.023。模型加载约 3.7–4.9 秒，进程 RSS 峰值约 2GiB；100 ms 采样的整卡显存峰值为 5.37–5.90GiB，包含约 4GiB 桌面及其他进程。本机短片段达到低于 8GB 的占用目标；未在物理显存小于 8GB 的显卡上验收，整课运行未采集显存峰值。CPU 与 CUDA 使用不同模型，速度对比包含模型差异。

## DeepSeek V4.1 Flash 与上下文缓存

四课最终短片段各 60 秒、2 章完成，图文最多两张候选/章。叔本华、抽象代数、CS336、软件工程实际请求数为 2、2、2、3（软件工程含一次结构修复），估价分别为 0.007610、0.006589、0.006776、0.009647 元，共 0.030621 元。本地 ASR 无云端费用。此前叔本华三章诊断调用另为 0.010602 元，本轮累计 0.041223 元；不包含已有历史任务。费用为当时空闲价格估算，以账单为准。

实际缓存输入分别为 3712/7924、5632/9336、5376/9056、9984/13119 token。追加完整前缀已在真实 Responses API 命中；同材料对照见下一节，不能把单次结果当作普遍净省费百分比。四段均通过结构、资源和当前章引用边界校验；内容抽查仍发现图文重复、文科无依据扩展及 review 噪声，需要继续按学习者视角改进，不能标为内容质量验收通过。

## 固定证据的缓存对照

复用 CS336 两章已保存的同一组转写、图片、章节范围与上章末尾，不重新识别或抽帧；每种策略各发两次请求，max_retries=0。独立逐章输入/缓存/输出为 7136/4864/993 token，估价 0.006341 元；追加历史为 9136/7040/824 token，估价 0.005533 元。两者都能命中固定前缀。单看输入估价约为 0.002369 与 0.002237 元，后者低约 5.6%；总价差异还包含随机输出长度变化，且服务端已有缓存，不能据此推断所有课程的净收益。两次对照合计 0.011874 元，至此本轮图文 API 估价累计 0.053097 元。

开发工具 `tools/replay_composition.py` 默认只离线导出证据包，已验证不创建图文请求；显式 `--live` 才付费，一次最多三章，不重试。它用于定点诊断和比较，未写入成功版本，不是任务恢复功能。


## 四个完整课时与学习者审阅

2026-09-14，在四课短片段回归后，各运行一次完整基线，全部使用本地 CUDA ASR、5 分钟目标章长、每章最多 6 张候选、4500 输出 token、每章最多一次修复；总调用上限按计划章数加 6。输入缓存只读，基线均保留。

| 课时 | 时长分钟 / 计划章 | ASR 秒 | 采样秒 | 图文秒 | 基线请求 / 成功章 | 基线估价元 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 叔本华 | 88.08 / 18 | 147.42 | 93.17 | 129.47 | 20 / 17 | 0.221972 |
| 抽象代数 | 44.96 / 9 | 65.89 | 106.17 | 65.09 | 10 / 9 | 0.103347 |
| CS336 | 81.04 / 16 | 109.34 | 188.09 | 102.99 | 16 / 16 | 0.192599 |
| 软件工程 | 99.99 / 20 | 162.19 | 149.56 | 172.02 | 23 / 18 | 0.243020 |

基线共约 314 分钟，ASR 约 485 秒；这是本机单次测量，不是吞吐保证。缓存输入/总输入分别为 481792/601992、325504/380461、458624/564159、399232/504503。三个较长课时发生预算重置，软件工程还发生一次校验重置；200K 预算不等于始终携带整课历史。

失败显式保留：叔本华第 11 章错引历史证据，软件工程第 3/9 章在结构及引用修复后仍失败。独立逐章回放共 3 次请求成功，额外 0.036846 元，没有重跑 ASR 或整课。软件工程这些请求也记录了服务用量，不能把失败算成免费。

通读四份完整讲义，对疑点查看原帧及转写后，生成四份独立校订稿，63/63 章完成；原始结构/资源与章节内引用检查通过。机械完成与内容正确分别记录，具体问题和阅读入口见[内容质量记录](content-quality.md)。新导出工具的离线用例同时检查原稿保留、图片携带、原证据更改拒绝、手改拒绝、原媒体目录保护与输出锁；最后两项加入后定点复验通过。

本轮所有图文 API 共 **91 次请求，估价 0.871781 元**：包含四课短片段、初次诊断、缓存对照、p0-5/p0-6 的局部比较、四个完整基线及三章补跑；全部有用量和计价记录，不含历史任务。ASR 全部在本地，无云端语音费用；也不含电费、权重下载和本地存储成本。费用仍以供应商账单为准。逐请求组汇总在 artifacts/evaluations/learner-review/summary.json。

本轮未重新运行全部课程验证最终提示：整课固定为 p0-5 基线；p0-6 在数学单章的开发版对照中未解决漏条件，最后的代码/性能措辞仅离线检查。校订稿中确定性修改来自本地人工审阅，未冒充模型自动改善。

## 2026-09-24：第六课端到端基线与固定证据对照

在 `d5f6365` 的生产实现上运行生成式软件工程第六课《需求和架构 (1)》，原视频 5,810.133562 秒（96 分 50 秒），H.264 1280×410。`inspect --decode` 的轨道探测和前中后抽查通过，没有运行 `inspect --full`。转换读取项目外缓存并在发布前核对素材指纹，输入未改写。

运行 `uv run --no-sync video-learner convert <第六课缓存目录> --profile programming --asr-secret secrets/qwen.secret --secret secrets/deepseek.secret --output output/software-lesson06-baseline-20260924`。除 profile 和凭据路径外使用默认配置：Qwen ASR、DeepSeek deepseek-flash、p0-6、jobs=1、180 秒分章、2 秒扫描、每章最多 12 图、完整历史上下文。本次没有更改生产生成逻辑。

| 项目 | 实际结果 |
| --- | --- |
| 退出与完成 | CLI 退出码 0，成功发布 r001；34/34 章完成 |
| 总时间 | CLI 墙钟总用时 579.0 秒；阶段之和 578.4 秒，差额为阶段间开销 |
| 语音识别 | 205 片，205 次请求，零重试；阶段 199.031 秒，请求累计 181.596 秒，音频准备 12.280 秒 |
| 截图 | 扫描 2,906 帧，保存 348 张完整候选；143.406 秒 |
| 图文整理 | 39 次请求，5 次修复重试；整体阶段 235.6 秒，逐章阶段累计 235.172 秒，最终 68 图 |
| 校验导出 | 0.640 秒 |
| 费用 | ASR 估算 1.257960 元，图文估算 0.724454 元，共 1.982414 元；采用随运行保存的价格快照，并非供应商账单 |

五次失败响应累计耗时 31.25 秒，均由已有重试修复：文字块携带 frame_id 一次、缺少 review 字段一次、JSON 非法转义一次、引用当前包之外的 ID 两次。上下文发生两次预算重置、两次引用修复重置。图文累计输入 1,016,909 token，其中缓存 829,696，输出 39,605；高缓存命中率不能据此视为净节省。

实际执行 `tools/replay_conversion.py`，34 章冻结输入、已采用草稿、结构化讲义与 Markdown 生成快照的只读重建通过。`tools/audit_coverage.py` 在第六课和旧第一课校订稿上运行通过；第六课实际送图由冻结输入统计，旧稿缺少冻结输入时显示未知。`tools/profile_conversion.py --local --sample-seconds 30` 完成本地音频、采样、整稿校验、渲染与独立导出回放，并检查原讲义字节未变。

本地 48:25–48:55 的采样回放为 0.867 秒，15 个扫描时刻保存 2 个候选，却调用 extract_frame 17 次；其中累计 0.748 秒，打开媒体累计约 0.060 秒。结果支持进一步测量重复解码和 seek 的开销，尚未实施优化、没有加速比例。本地回放未控制冷缓存，含 cProfile 开销，不直接外推整课。ASR jobs=4 的真实对照也尚未运行。

固定证据独立逐章对照共 3 次图文请求，估算 0.027506 元：第 1 章结构校验失败，原始响应保留；第 16、28 章通过校验，写到独立评估目录，没有替换整课。连同基线本轮总估算约 2.009920 元。具体阅读缺陷和对照限制见[内容质量记录](content-quality.md#2026-09-24生成式软件工程第六课基线)。

产物与性能、覆盖及对照原始数据分别位于 `output/software-lesson06-baseline-20260924/`、`artifacts/evaluations/lesson06-20260924/`，均不提交仓库。新增只读覆盖工具通过 Ruff 规则、格式及实际运行检查，受影响文档的本地链接核对通过。生产代码与测试未改变，本轮未重跑全套 pytest，未复验文字修订或手改保护；本次机械成功不改变 M5 人工内容质量验收仍未完成的状态。
## 2026-09-24：质量改进与独立复审验证

- 离线检查：`uv run --no-sync pytest -q --tb=short`，110 passed；Ruff 检查和格式检查通过。新增覆盖正文编号/空标题、前后边界与帧窗口、两家模型的独立复审与有界修复、自动图块定位、非目标 CRLF/手改保护、纯图章节、取消/失败保留初稿、并发编辑与复审重放。
- 第六课 42:00–50:00 真实样本：Qwen ASR 18 片、32 张候选、3 章初稿完成。早期双份选图契约的复审失败保留 r001；改为本地应用逐图决定后，单独复审 4 次请求（1 次修复）完成 r002，估算 0.047993 CNY。离线重放通过，图片依赖完整。
- 固定旧 r001 证据：DeepSeek 分别复审 ch-001/020/028/031，保存 r003/r004/r005/r006，共 5 次请求，估算 0.051997 CNY；四版均离线重放通过。Qwen 单独复审 ch-028，1 次请求，r007 估算 0.015946 CNY，重放通过。内容改善与漏检见 content-quality.md；成功返回和重放不等于内容验收通过。
- 未采用试验：早期第 16 章复审 3 次后因选图/补丁不一致失败；增加反馈后早期 r002 完成。DeepSeek `reasoning_effort=low`、12000 输出 token 的同章试验达到输出上限，未登记新版本，也未改变默认思考设置。

### 整课与最终流程

第六课完整范围 0–5810.133562 秒写入 `output/software-lesson06-quality-20260924/`。Qwen ASR 205 片，扫描 2906 帧，保留 348 张候选；p0-7 初稿 34 章、39 图，34 次图文请求无修复。提取、并发和解码设置未改；整课配置只将图文调用预算设为 120，为新增复审预留额度。

首次整课复审在 ch-027 因图片落点类型不符失败，包含初稿的该次运行共 70 次图文请求，估算总费用 1.923783 CNY，r001 保留。单章复验生成 r002；第二次完整复审在 ch-031 遇同类错误，39 次请求、估算 0.413684 CNY，没有登记新版本。明确原图位置锚点后重新完整复审，40 次请求（6 次修复）、175.2 秒、估算 0.420148 CNY，CLI 退出 0，成功生成 r003。r001 与 r003 均通过 34 章离线重放，r001 当前字节与生成快照一致，r003 的 94 张本地图片依赖完整。

最终代码另从原视频 43:48–46:18 一次执行 `convert`：6 段 ASR、13 张候选、1 章，初稿与复审各 1 次图文请求，无重试，22.9 秒、估算 0.056375 CNY；退出 0 并直接返回 r002。目录为 `output/software-lesson06-quality-final-smoke-20260924/`，验证了默认自动复审的完整入口。

自动复审仍存在已确认的内容漏检和误改。Qwen 文字修订试验形成 r004，但未完整落实第 25 章要求；随后单块修订因返回多个块被拒绝，未登记后续版本。最终基于 r003 对照原图和转写直接校订四处内容，再通过现有 `tools/export_review.py` 零 API 导出 `output/software-lesson06-quality-reading-20260924/`；这是阅读副本，不是 CLI 修订版本。34 章、95 图、271 个有效锚点、3 条核对提示，全部图片依赖存在；原始转写、帧、章节范围未变。完整记录和覆盖报告位于 `artifacts/evaluations/lesson06-quality-20260924/`，正文改善与限制见 content-quality.md。


## 2026-09-24：按职能组合复审验证

- Windows / Python 3.12，默认离线测试 118 项通过；Ruff 检查及格式检查通过。新增验证覆盖顺序状态传递、同职能重复插入的 ID、函数注入、纯函数输入保护、跨轮疑点保留和显式解决、报告模式与职能限制、第二轮失败/取消保留初稿，以及旧正文缺陷留给内容轮。配置示例可加载，本地文档链接和差异空白检查通过。
- 第六课 43:48–46:18 执行真实 convert，目录 `output/software-lesson06-modular-smoke-20260924/`：6 段 Qwen ASR、13 张候选、1 章；DeepSeek 初稿 1 次请求，图文轮 3 次（2 次校验重试），内容轮 1 次，全部成功后直接返回 r002。总耗时 32.4 秒，按配置估算 0.121528 CNY；初稿 0 图、复审版 6 图。重试分别拒绝多余字段及不完整候选判断，没有采用违规输出。
- 基于该 r002，用自定义 TOML 仅配置 `lecture_style` 内容轮、`mode="report"` 和文体审阅指令，选择 Qwen 实跑 revise --review：1 次请求无重试，17.0 秒、估算 0.014129 CNY，生成 r003。r002/r003 的 notes.md 逐字相同，6 张图片依赖均存在，报告增加了两条模型意见。
- 新 r001、两轮 r002、自定义只报告 r003 都完成离线重放；旧整课 `software-lesson06-quality-20260924` 的单轮 r003 仍通过 34 章重放。新版覆盖工具成功读取两轮图片并集。自定义配置和报告位于忽略的 `artifacts/evaluations/lesson06-modular-20260924/`。
- 本次验证模块化契约与短片段实跑，没有重跑两轮复审的完整课程，也没有完成配图和内容的逐项人工标注；模型报告噪声见 content-quality.md。

## 2026-09-24：Markdown、HTML、PDF 导出验证

- Windows / Python 3.12，`uv run --no-sync pytest -q`：125 项通过；Ruff 规则、格式和 `uv lock --check` 通过。新增用例验证可携带目录离线导出、CRLF 与手改原字节、共享索引再次导出、HTML 静态化及公式、PDF 中文/长表/代码分页与公式回退、无效图片和路径、后续适配器失败不发布、显式基线与输出保护。PDF 用例在可选依赖和中文字体可用时执行，本机全部运行。
- 以第六课人工校订阅读副本 `output/software-lesson06-quality-reading-20260924/` 为输入，一次 CLI 导出三种格式至 `output/pdf/lesson06-20260924/`，退出码 0。34 章、95 张截图；导出 Markdown 与输入逐字节一致，图片、`notes.json` 和 `source.json` 完整。本次没有原视频解码、语音识别或模型请求。
- HTML 在独立无头 Edge 中离线打开，95 张图片全部加载、4 个 MathML 公式；1440 像素桌面和 390 像素手机宽度均无整页横向溢出，已查看两种宽度及正文中段截图。
- PDF 共 73 张 A4 页面，含 95 张截图和 3 张公式图片。Poppler 渲染全部页面，查看五份覆盖全页的拼图，并放大检查代码页、公式页和来源长表页；未见截断、重叠或空白缺图。最终产物重新渲染的 73 张页面图片与已检查版面逐字节相同。评估图片位于 `artifacts/evaluations/html-pdf-20260924/`，均不提交。
- 一处含 `\bmod` 的公式超出当前 Mathtext 子集，PDF 第 39 页显式保留完整 LaTeX 原文，CLI 与 `export.json` 均给出提示；HTML 可渲染。此次验证覆盖 Windows 字体和本课排版，不代表任意 LaTeX、其他平台字体或全部浏览器均已验收。仅验证导出内容保留和版面，未新增课程事实正确性审阅，也未做性能优化。
